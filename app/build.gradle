plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
}

project.ext.lwjglVersion = "3.3.3"
project.ext.lwjglNatives = "natives-windows"

dependencies {
    // LWJGL core modules
    implementation platform("org.lwjgl:lwjgl-bom:$lwjglVersion")
    implementation "org.lwjgl:lwjgl"
    implementation "org.lwjgl:lwjgl-assimp"
    implementation "org.lwjgl:lwjgl-glfw"
    implementation "org.lwjgl:lwjgl-opengl"
    implementation "org.lwjgl:lwjgl-stb"

    // LWJGL native libraries for Windows
    runtimeOnly "org.lwjgl:lwjgl::$lwjglNatives"
    runtimeOnly "org.lwjgl:lwjgl-assimp::$lwjglNatives"
    runtimeOnly "org.lwjgl:lwjgl-glfw::$lwjglNatives"
    runtimeOnly "org.lwjgl:lwjgl-opengl::$lwjglNatives"
    runtimeOnly "org.lwjgl:lwjgl-stb::$lwjglNatives"
    
    // glTF support for GLB models
    implementation 'de.javagl:jgltf-model:2.0.3'
    implementation 'de.javagl:jgltf-impl-v2:2.0.3'
}

java {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11
}

application {
    mainClass = 'mystuff.game.Game'
}

run {
    // Enable OpenGL debug output
    systemProperty "org.lwjgl.util.Debug", "true"
    // Enable LWJGL debug mode
    systemProperty "org.lwjgl.util.DebugLoader", "true"
}

// Task to extract textures from GLB files
task extractTextures(type: JavaExec) {
    classpath = sourceSets.main.runtimeClasspath
    mainClass = 'mystuff.utils.GLBTextureExtractor'
    
    // Default arguments - can be overridden
    def glbFile = project.hasProperty('glbFile') ? project.glbFile : 'models/Quequis_House.glb'
    def outputDir = project.hasProperty('outputDir') ? project.outputDir : 'src/main/resources/textures/extracted'
    
    args = [glbFile, outputDir]
    
    doFirst {
        println "Extracting textures from: $glbFile"
        println "Output directory: $outputDir"
    }
}
